# k8s-config/k8s/databases/postgres-project-db-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-project-db-deployment
spec:
  selector:
    matchLabels:
      app: postgres-project-db
  template:
    metadata:
      labels:
        app: postgres-project-db
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: project_db
        - name: POSTGRES_USER
          value: project_user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: project-secrets
              key: DB_PASS
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-project-db-service
spec:
  type: ClusterIP
  selector:
    app: postgres-project-db
  ports:
  - port: 5432
    targetPort: 5432
